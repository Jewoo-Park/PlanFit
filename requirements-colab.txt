# Colab / notebook: use the preinstalled torch / torchvision / torchaudio stack.
# Do not install torch here — pip would replace Colab's CUDA-matched wheels and break torchvision/torchaudio.
#
# If you see: RuntimeError: operator torchvision::nms does not exist
#   → Restart runtime, then reinstall torch+torchvision+torchaudio together (see README Colab section).
PyYAML>=6.0,<7
transformers>=4.44.0,<5
accelerate>=0.33.0,<1
tqdm>=4.66.0,<5
huggingface_hub>=0.24.0,<1
hf_transfer>=0.1.8,<1
langgraph>=0.2,<1
